import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF00A9B8); // A vibrant teal
  static const Color primaryDark = Color(0xFF007A8A);
  static const Color accent = Color(0xFFFFC107); // Bright yellow for highlights
  static const Color background = Color(0xFFF0F4F8); // Light, clean background
  static const Color surface = Color(0xFFFFFFFF); // For cards and dialogs
  
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);

  static const Color correct = Color(0xFF4CAF50);
  static const Color incorrect = Color(0xFFF44336);

  static const Color locked = Color(0xFFBDBDBD);
  static const Color inProgress = Colors.blueAccent;
  static const Color completed = Color(0xFF4CAF50);
}